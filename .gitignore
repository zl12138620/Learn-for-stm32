# CMake 构建产物
build/
build-*/

# 临时日志
*.log

# Python
# 通用忽略 *.py, 但下面这些例外必须留着:
# 它们是**生成脚本** —— 生成物都进了仓库, 生成器不进的话,
# 以后想加个字都没法重新生成, 只能照着重写一遍。
#
# ⚠ 加了新的生成脚本记得往这里再补一行。这个坑踩过一次了:
#   当时 CN_Font.c(生成物)进了仓库, 而 gen_cn_font.py(生成器)被 *.py 挡在外面,
#   等于把源码和一个没法复现的二进制一起提交了。
*.py
!工具/gen_cn_font.py
!工具/gen_lvgl_font.py
__pycache__/
*.pyc

# 本地资料与可重新下载的工具包(不纳入仓库)
资料/
tools/*.vsix
pyocd/*.pack
学习笔记/
