# 工程完整性检查报告

**工程**：STM32F407VGT6（立创·天空星开发板）
**分支**：`EXTI_NVIC`
**检查日期**：2026-09-13
**工具链**：arm-none-eabi-gcc 13.3.1 · CMake 3.x + Ninja · STM32F4xx_DSP_StdPeriph_Lib V1.9.0
**器件宏**：`STM32F40_41xxx` + `USE_STDPERIPH_DRIVER`（已确认与实物芯片一致）

---

## 一、结论

**工程可以完整编译链接，不存在"缺文件导致构建失败"的情况。**

干净全量编译的结果：

```
[43/43] Linking C executable output\STM32F407VGT6.elf

Memory region         Used Size  Region Size  %age Used
           FLASH:        5264 B         1 MB      0.50%
             RAM:        1684 B       128 KB      1.28%
          CCMRAM:           0 B        64 KB      0.00%
   text     data      bss      dec      hex
   5256        8     1684     6948     1b24
```

产物齐全：`build/output/STM32F407VGT6.{elf,hex,bin,map}`

但检查中发现 **1 个缺失的库目录**、**1 处一定会踩的引脚冲突**、以及若干隐患，分述如下。

---

## 二、缺失的库目录：`Libraries/CMSIS/Device/`

标准 V1.9.0 发行包中应有：

```
STM32F4xx_DSP_StdPeriph_Lib_V1.9.0/Libraries/CMSIS/
├── Include/          ✅ 存在（core_cm4.h 等 13 个头文件）
├── RTOS/             ✅ 存在
└── Device/           ❌ 整个目录缺失
    └── ST/STM32F4xx/
        ├── Include/  ← stm32f4xx.h / system_stm32f4xx.h
        └── Source/Templates/
            ├── system_stm32f4xx.c
            └── arm/startup_stm32f40_41xxx.s ...
```

**影响**：不影响构建。本工程用自备副本顶上了这个缺口：

| 本该在库里 | 本工程实际位置 |
|---|---|
| `Device/.../Include/stm32f4xx.h` | [System/stm32f4xx.h](System/stm32f4xx.h) |
| `Device/.../Include/system_stm32f4xx.h` | [System/system_stm32f4xx.h](System/system_stm32f4xx.h) |
| `Device/.../Source/Templates/system_stm32f4xx.c` | [System/system_stm32f4xx.c](System/system_stm32f4xx.c) |
| `Device/.../Source/Templates/arm/startup_*.s` | [Startup/](Startup/) |

**必须注意的坑**：[System/stm32f4xx.h:137](System/stm32f4xx.h#L137) 是**被改过的私有副本**，`HSE_VALUE` 已从 ST 默认的 25 MHz 改成 8 MHz：

```c
#define HSE_VALUE  ((uint32_t)8000000)  /* 天空星 F407VGT6 板载 8MHz 晶振(原 ST 默认 25MHz 已改) */
```

一旦有人从这个目录拷库进来覆盖（或对照官方包 diff），时钟会算错，**串口波特率、定时器频率会全部歪掉**，而且症状是"能跑但时序不对"，很难查。建议在文件头显式加一行醒目注释，或把 HSE 改成由 CMake 传入的 `-DHSE_VALUE=8000000`，让改动显式化。

---

## 三、🔴 引脚冲突：OLED 与编码器争 PB7/PB8

这是本次检查**最需要你决策**的问题。两个模块抢同一对引脚，而且是完全相反的配置：

| 模块 | 位置 | PB7 | PB8 | GPIO 配置 |
|---|---|---|---|---|
| OLED（SSD1306 软 I2C） | [OLED.c:16-17](SoftWare/src/OLED.c#L16-L17) | SCL | SDA | **开漏输出** + 上拉 |
| 编码器（增量式两相） | [Encoder.h:40-41](SoftWare/inc/Encoder.h#L40-L41) | B 相 | A 相 | **上拉输入** + EXTI8 下降沿 |

**当前为什么没炸**：[main.c:31](USER/main.c#L31) 只调用了 `OLED_Init()`，没有调用 `Encoder_Init()`。所以 OLED 正常工作。

**什么时候会炸**：只要在 OLED 之后加上 `Encoder_Init()`，它会执行

```c
GPIO_InitStructure.GPIO_Pin  = ENC_A_PIN | ENC_B_PIN;   /* PB8 | PB7 */
GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IN;            /* 输入！ */
```

把 PB7/PB8 重配成**输入**——OLED 的 SCL/SDA 立刻失去驱动能力，**屏幕直接黑掉**。反过来先 `Encoder_Init()` 再 `OLED_Init()` 则编码器失效。当前分支名叫 `EXTI_NVIC`，说明你正往编码器方向走，这个坑大概率会在下一步踩到。

### 三种处理方式

**方案 A —— 编码器换到 PB5/PB6（推荐，若要两个同时用）**

关键点：EXTI 线号等于引脚号，而 **PB5、PB6 都落在 `EXTI9_5` 组**，所以中断向量名**不用改**，只改 3 个宏：

```c
#define ENC_A_PIN   GPIO_Pin_6      /* A 相 = PB6, 原 PB8 */
#define ENC_B_PIN   GPIO_Pin_5      /* B 相 = PB5, 原 PB7 */
#define ENC_EXTI_LINE   EXTI_Line6  /* 原 EXTI_Line8 */
/* ENC_EXTI_IRQn 保持 EXTI9_5_IRQn 不变 */
```
[Encoder.c:100](SoftWare/src/Encoder.c#L100) 的 `EXTI9_5_IRQHandler` 和 `EXTI->IMR` 屏蔽逻辑无需改动。OLED 继续用 PB7/PB8。

**方案 B —— 若本来就不打算同时用**（比如两者共用同一个排针接口）

那就保持现状，但建议加一道**显式防呆**，避免以后自己误加：

```c
/* 在 main.c 里二选一，另一句注释掉并写明原因 */
OLED_Init();          /* 用屏幕：PB7/PB8 = 软 I2C */
/* Encoder_Init(); */ /* 用编码器：PB7/PB8 = EXTI 输入，与 OLED 互斥！ */
```

**方案 C —— OLED 让位**，改 [OLED.c](SoftWare/src/OLED.c#L16-L17) 的宏和 `OLED_I2C_Init()` 里的 `GPIO_Pin_7/8`，把编码器留在 PB7/PB8。

> 我这次**没有改动任何相关代码**，等你看过实际接线再定。

---

## 四、其它隐患（不阻断构建）

### 4.1 `OLED_F8x16` 把「定义」写在了头文件里

[SoftWare/inc/OLED_Font.h](SoftWare/inc/OLED_Font.h) 里是**定义**而非声明：

```c
const uint8_t OLED_F8x16[][16] = { ... };   /* 这是定义！ */
```

目前只有 [OLED.c:2](SoftWare/src/OLED.c#L2) 一个 `.c` 包含它，所以侥幸没事。**第二个 `.c` 再 `#include "OLED_Font.h"` 就会在链接期报 `multiple definition of 'OLED_F8x16'`**。

正确写法是把数组搬进 `OLED_Font.c`，头文件里只留：

```c
extern const uint8_t OLED_F8x16[][16];
```

### 4.2 `Startup/` 里 13 个启动文件大多不可用

只有 `startup_stm32f40_41xxx_gcc.S` 是 GCC 语法且真正参与编译（[CMakeLists.txt:77](CMakeLists.txt#L77)）。其余 12 个都是 **Keil/MDK 语法**（`AREA` / `EQU` / `EXPORT` / `DCD`），用 `arm-none-eabi-gcc` 编译必炸。别误换。

### 4.3 游离文件

- `USER/main.c.bak_before_keyled`、`USER/main.c.bak_user_editing` —— 备份文件。后缀是 `.c.bak_*` 而非 `.c`，**不会被 `file(GLOB .../*.c)` 收进来**，安全。
- `USER/output/` —— 空的游离目录，可删。

### 4.4 编译告警

全量编译有若干告警，**全部来自 ST 库**（`System/system_stm32f4xx.c` 的 `-Wmisleading-indentation`、`stm32f4xx.h` 里 `DBGMCU_APB2_FZ_*` 宏重定义）。这属于库固有噪声，工程已用 `include_directories(SYSTEM ...)`（[CMakeLists.txt:58](CMakeLists.txt#L58)）把库头的告警压掉，处理得当，**不建议去改库**。

---

## 五、本次已补齐的空壳模块

检查发现两个"声明了但没实现"的模块，已按你的选择补齐：

### 5.1 [SoftWare/src/SoftI2C.c](SoftWare/src/SoftI2C.c) —— 原为 **0 字节空文件**

原 [SoftI2C.h](SoftWare/inc/SoftI2C.h) 只有一句 include guard，零声明。现已补成**通用软件 I2C 主机**：

| 项目 | 说明 |
|---|---|
| 引脚 | **由调用者通过 `SoftI2C_Bus_t` 提供，模块不占用任何固定引脚** |
| 端口 | SCL / SDA 可分别挂在不同的 GPIO 端口 |
| 核心机制 | 开漏输出 + 上拉；输出模式下 IDR 仍可读，**收发无需切换方向** |
| 时序 | 完整 START / 重复START / STOP / ACK / NACK |
| API | `Init` `Start` `Stop` `SendByte` `RecvByte` `Probe` `WriteReg(s)` `ReadReg(s)` `Scan` |
| 返回值 | 1 = ACK/成功，0 = NACK/失败 |

**未接入 OLED** —— 按你的选择，`OLED.c` 一行没动（它刚在最近提交里调通，零回归风险）。等哪天真要用，把 `dev_addr` 填 `0x3C`、SCL/SDA 填 PB7/PB8 即可。

> 注意：该模块为**阻塞实现，不可在中断里调用**；无总线仲裁与时钟拉伸处理，仅适合单主机场景。

### 5.2 [SoftWare/src/SoftWarePWM.c](SoftWare/src/SoftWarePWM.c) —— 原仅 1 行 `#include`

原文件只有 `#include "stm32f4xx.h"`（24 字节），但 [SoftWarePWM.h:5](SoftWare/inc/SoftWarePWM.h#L5) 声明了 `void PWM_Init(void);` —— **无定义**。现在没人调用所以能过链接，一旦调用立刻 `undefined reference to 'PWM_Init'`。

已按你的选择实现为 **PB4（板载蜂鸣器 FMQ）/ TIM3_CH1** 硬件 PWM：

| 项目 | 说明 |
|---|---|
| 时基 | APB1 定时器时钟 84 MHz ÷ 84 = **计数频率 1 MHz**（1 计数 = 1 µs） |
| 频率范围 | 约 **16 Hz ~ 1 MHz**（16 bit ARR），覆盖蜂鸣器全音域 |
| 输出 | PWM1 模式、高电平有效；**ARR/CCR 都开预装载**，运行中改参数无毛刺 |
| API | `PWM_Init` `PWM_Start` `PWM_Stop` `PWM_SetFreq` `PWM_SetDuty` `PWM_GetFreq` `PWM_GetDuty` |

关于 PB4 的 JTAG 问题已核实：**STM32F4 没有 F1 的 AFIO / SWJ_CFG 寄存器**，`GPIO_PinRemapConfig` / `GPIO_Remap_SWJ_JTAGDisable` 在这个库里**根本不存在**。手册 33.4.4 节说明 PB4 只需配置 `GPIO_MODER` 即可释放 NJTRST，代码里正常走 `GPIO_PinAFConfig` + `GPIO_Init` 即可，无需任何特殊处理。**唯一前提**：调试口必须用 SWD（`SWCLK=PA14 / SWDIO=PA13`），若改用 JTAG，PB4 会被调试口占回去。

> ⚠️ `PWM_Init()` 会**直接让蜂鸣器以 1 kHz 响起来**（与 `OLED_Init()` 立刻显示类似，方便一眼确认硬件通了）。不想上电就响，紧跟一句 `PWM_Stop()`。

---

## 六、外设资源分配总表

目前占用的资源（补实现时已避开冲突）：

| 外设 / 引脚 | 占用模块 | 备注 |
|---|---|---|
| USART1 (PA9/PA10) | [USER/main.c](USER/main.c) | 波特率 9600 |
| 软串口 TX/RX (PA2/PA3) | [SoftUSART.c](SoftWare/src/SoftUSART.c) | **TIM6** + EXTI3 |
| 编码器 A/B 相 (PB8/PB7) | [Encoder.c](SoftWare/src/Encoder.c) | **TIM7** + EXTI9_5，⚠️ 与 OLED 冲突 |
| OLED SCL/SDA (PB7/PB8) | [OLED.c](SoftWare/src/OLED.c) | 软 I2C，⚠️ 与编码器冲突 |
| **蜂鸣器 FMQ (PB4)** | [SoftWarePWM.c](SoftWare/src/SoftWarePWM.c) | **TIM3_CH1** ← 本次新增 |
| LED2 (PB2) | 未使用 | 低电平点亮（高边经 R29 2k 接 3V3）|
| 按键 SW2 (PA0) | 未使用 | WKUP |
| SysTick | main.c `Delay_ms` | 忙等，用完即关 |

**定时器占用**：`TIM6` = SoftUSART，`TIM7` = Encoder，`TIM3` = PWM。**TIM1/2/4/5/8 空闲。**

---

## 七、验证记录

| 检查项 | 结果 |
|---|---|
| 干净全量编译（43 个编译单元） | ✅ 通过 |
| 链接 | ✅ 通过，无 undefined reference |
| 产物 `.elf/.hex/.bin/.map` | ✅ 全部生成于 `build/output/` |
| `SoftI2C.c` 编译告警 | ✅ **0 条**（`-Wall -Wextra -Wpedantic`） |
| `SoftWarePWM.c` 编译告警 | ✅ **0 条**（同上） |
| 符号链接性 | ✅ 辅助函数为 `static`（`t`/`d`），API 为全局（`T`） |
| Flash / RAM 占用 | 5264 B (0.50%) / 1684 B (1.28%) |

> 注：两个新模块目前**没有代码调用它们**，链接器的 `--gc-sections` 会把它们从最终固件里剔除，所以 Flash 占用与补齐前完全一致（5264 B）。这是预期行为，不是没编译进去——`.obj` 已生成且符号齐全。

---

## 八、建议的下一步

1. **先决策 PB7/PB8 冲突**（第三节），这是唯一会真正让功能失效的问题。
2. 想验证 PWM：在 `main.c` 的 `while(1)` 前加
   ```c
   PWM_Init();                        /* 1kHz 响一声, 确认蜂鸣器通了 */
   /* PWM_SetFreq(2000); PWM_SetDuty(50); */
   ```
3. 顺手把 `OLED_F8x16` 从 [OLED_Font.h](SoftWare/inc/OLED_Font.h) 挪到 `.c` 里，消除重复定义隐患（4.1 节）。
4. `SoftI2C` 目前是"备而不用的通用模块"。若不打算抽公共 I2C，也可以直接删掉 `SoftI2C.c/h`，免得日后困惑"这文件到底是干嘛的"。
